{
  "name": "your-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build":"electron-packager ./ HelloWorld --platform=win32 --arch=x64 --icon=icon.ico --out=./build --asar --app-version=0.0.1"
  },
  "devDependencies": {
    "electron": "^7.1.9"
  },
  "dependencies": {
    "glob": "^7.1.6"
  }
}
